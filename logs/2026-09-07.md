# Daily Log — 2026-09-07 (Day 6, Phase 0) — covers 09-04 → 09-07

## Context
Day 2's briefing (09-03) rolled over: items 5–7 unfinished. 09-04 partial (paper exercises,
git rescue). 09-05 partial (sqrt_newton started). 09-06 no work (guests). 09-07 resumed.
Failure protocol applied: resume, no spiral.

## Completed across the span
| Item | Evidence |
|---|---|
| Vol 02 Ch 2 paper exercises E1–H6 | exercises/vol-02/ch02/p1–p4 (H7 revealed after attempts; cold reproduction owed) |
| Newton square-root step **derived by learner** from two-guess intuition | chat record 09-07; docstring in sqrt_newton.py |
| `sqrt_newton()` from scratch, gated | src/mathx/sqrt_newton.py — sabotage test passed (lying test exposed) |
| `variance()`, `std_dev()` from scratch, gated | src/stats/variance.py — full spec, H7 identity cross-check |
| Package layout: code/ → src/, `__init__.py`, `python -m` | commit 35d7e9a |
| Git rescue: shift-deleted files recovered (restore, cached blobs, misplaced mean.py) | 09-04 |
| .gitignore (after `__pychache__` typo hunt) | commits 293b267, ebe1a32, 35d7e9a |
| Rules 9 (derivation law) and 10 (Radar Hour) added | ROADMAP.md |

## Debrief — learner fills these
- Two-machine sync: laptop hash ___ == VM hash ___ (paste both)
- **Var(A) arithmetic error autopsy.** On the first paper attempt at Vol 02 Ch 2 exercise M5
  (photo p2), the variance of Dataset A {0, 5, 25} was written as 6.66. The correct value is
  350/3 ≈ 116.67, which the chapter §4 computes explicitly. The later sheet (p4) uses 116.66.
  One sentence: what computation produced 6.66 the first time?<br>
A. Calculating mean absolute deviation and not mean squared deviation + Miscalculation of the mean(confidently added a wrong number because I did mental math hah).
- **Exercise M4 — "mean 20, variance 0, how many elements?"** First answer was "2 elements";
  established that n cannot be determined. One sentence, own words: why is n unknowable from
  the mean and variance alone?<br>
A. Because multiple datasets with different numbers of elements can have the same mean and variance.
- **`variance([5])` defense.** The code raises ValueError for a single element, and the count
  check runs *after* the loop rather than before. Why raise? Why check after?<br>
A. Raise because variance is undefined for a single element. Check before would mean two passes over the data, which is less efficient.
- **Why does `std_dev` need no guard of its own?** (It calls `variance()`, which guards.)<br>
A. It guards against both empty sequence and sequence with less than 2 elements as variance cannot be computed for such cases.
- **`except: pass` around an assert** — what does it do to the test, in one sentence?<br>
A. It suppresses the AssertionError, making the test appear to pass even if the assertion fails.
- **Sabotage test lesson.** In `sqrt_newton.py`, `return new_guess` was changed to
  `return new_guess + 1`. The test wrapped in `try/except AssertionError: pass` still printed
  "all passed"; the bare assert failed with `AssertionError: 25`. What did this teach about
  trusting a green test?<br>
A. That a green test can be misleading if exceptions are suppressed; always verify the test logic itself.
- **Assessment (honest, 3 lines, own verdict on 09-04 → 09-07):** what went well, what didn't,
  rating /10.<br>
A. 5/10. I saw everything to the end but many mistakes were made especially with me overlooking many details and making repeat offenses. I tend to jump the gun and rush through exercises without double-checking my work.
- **Weak spots:** 2–3 specific things from this week's material you'd be nervous to be quizzed
  on right now.<br>
A. Oversight<br>
   Rushing through exercises without double-checking work.<br>
   Overconfidence in understanding without verifying details.<br>
   Lack of patience in thoroughly understanding concepts before moving on.<br>
   Difficulty in maintaining consistent focus throughout exercises.<br>
   Procrastination in addressing mistakes promptly.<br>


## Mentor notes
- Strengths shown: derived Newton step; self-diagnosed mkdir/mv messes; generalized try/except
  pattern unprompted; noticed stray `sq` file; asked "what is new_guess / 1e-6" instead of nodding.
- Habit tracker: ① proof-with-done — improved late in day. ② half-applied feedback — 4 strikes
  09-07 morning, then two clean rounds; checklist method works, keep it. ⑤ detour-at-dawn —
  3 consecutive mornings (physical AI, brand, staying current). Named; watch.
- Typos shipped to history this week: `chpter04`, `standar`, `__pychache__` (the last one had
  consequences: commit 5ac364a pushed a .pyc + an early sum()/len() variance.py). Read-aloud
  rule is not optional.
- `git add <folder>` without reading status: twice in one day (settings.json, .pyc).
- Arithmetic/labels: variance vs SD swapped 3× on paper; fixed by end of exercises.

## Owed → tomorrow's briefing
1. H7 proof reproduced cold on paper (photo, committed immediately)
2. Anki: variance-vs-SD, moments ladder, sqrt = x**0.5 via exponent law, 1e-6 notation,
   `except: pass` lie, shift/scale laws, sys.path, `git check-ignore`
3. First Radar Hour (rolled from 09-06) — Sunday 09-13 or a 60-min slot tomorrow
4. Linux II (permissions, processes) — Phase 0 resumes
5. Commit-message template setup (promised 09-03, still pending)
